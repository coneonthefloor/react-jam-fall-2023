import type { Meta, StoryObj } from '@storybook/react'

import CanvasBackgroundLayout from './CanvasBackgroundLayout'

const meta: Meta<typeof CanvasBackgroundLayout> = {
  component: CanvasBackgroundLayout,
}

export default meta

type Story = StoryObj<typeof CanvasBackgroundLayout>

export const Primary: Story = {}
