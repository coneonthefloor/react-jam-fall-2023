import type { Meta, StoryObj } from '@storybook/react'

import MenuScreenLayout from './MenuScreenLayout'

const meta: Meta<typeof MenuScreenLayout> = {
  component: MenuScreenLayout,
}

export default meta

type Story = StoryObj<typeof MenuScreenLayout>

export const Primary: Story = {}
