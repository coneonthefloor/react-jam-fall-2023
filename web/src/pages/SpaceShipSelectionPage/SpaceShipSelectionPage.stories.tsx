import type { Meta, StoryObj } from '@storybook/react'

import SpaceShipSelectionPage from './SpaceShipSelectionPage'

const meta: Meta<typeof SpaceShipSelectionPage> = {
  component: SpaceShipSelectionPage,
}

export default meta

type Story = StoryObj<typeof SpaceShipSelectionPage>

export const Primary: Story = {}
